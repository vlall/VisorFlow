# import stuff and run a main. we're gonna pass this main through a d3 graph in a bit.
# figure out how to import modules. 
# move this file to another folder that will import modules from the api folder.

import facebook, lastfm, spotify, twitter, wikipedia, youtube

class makeAll:
	def __init__ (self, )

	artist = Spotify('cher')
	print artist.display_name() 
	print artist.spotify_tags()
	printpretty.prettyPrint(artist.data)  

	# Change pretty print into something very JSON friendly.
